plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    compileSdkVersion 33 // Use the latest SDK version you have
    
    defaultConfig {
        targetSdkVersion 33 // Match this with compileSdkVersion
        
    }
}
